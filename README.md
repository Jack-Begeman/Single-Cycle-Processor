To run the test code, make sure you have iverilog installed and enter in:

$ iverilog -o test SingleCycleProcTest.v SingleCycleProc.v

This will create an executable file called "test". To run the test:

$ ./test
